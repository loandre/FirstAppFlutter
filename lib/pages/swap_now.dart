// swap_now.dart

import 'package:flutter/material.dart';

class SwapNowPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Swap Now'),
      ),
      body: Center(
        child: Text('Swap your assets here!'),
      ),
    );
  }
}
